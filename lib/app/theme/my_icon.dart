import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:flutter_vector_icons/flutter_vector_icons.dart';

// module user info
const Icon iconEmailBlue = Icon(
  Icons.mail_outline,
  color: Colors.blue,
);
const Icon iconLockBlue = Icon(
  Icons.lock_outline,
  color: Colors.blue,
);
const Icon iconFullNameBlue = Icon(
  FontAwesome.user_o,
  color: Colors.blue,
);
const Icon iconPhoneBlue = Icon(
  Icons.phone_outlined,
  color: Colors.blue,
);
const Icon iconHomeBlue = Icon(
  // Icons.house,
  FontAwesome.home,
  color: Colors.blue,
);
const Icon iconArrowBackBlue = Icon(
  Icons.arrow_back_ios,
  color: Colors.blue,
);
const Icon iconCommentBlue = Icon(
  FontAwesome.comment,
  color: Color(0xff2B9DFF),
);

// nav bar
const Icon iconNotificationNavBar = Icon(
  FontAwesome.bell,
  color: Color(0xffA2BAC4),
  size: 30,
);
const Icon iconCommentNavBar = Icon(
  FontAwesome.comment,
  color: Color(0xffA2BAC4),
  size: 30,
);
const Icon iconPlus = Icon(FontAwesome.plus);

// app bar
const Icon iconHomeAppBar = Icon(Icons.home_outlined);
const Icon iconUserAppBar = Icon(FontAwesome.user_circle);
